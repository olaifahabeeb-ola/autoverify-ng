# Development/testing dependencies only — not needed to run the app itself.
# Install alongside requirements.txt when working on the codebase or running CI:
#     pip install -r requirements.txt -r requirements-dev.txt

pytest>=8.0.0
pytest-cov>=5.0.0
psycopg[binary]>=3.1,<4.0